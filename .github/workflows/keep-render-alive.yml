name: Keep Render Alive

on:
  schedule:
    - cron: '*/30 * * * *'  # каждые 5 минут (UTC)
  workflow_dispatch:       # + ручной запуск по кнопке

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Bot
        run: |
          curl -f https://dashboard.render.com/web/srv-d826jabtqb8s73caiqog\
          || echo "⚠️ Ping failed at $(date)
